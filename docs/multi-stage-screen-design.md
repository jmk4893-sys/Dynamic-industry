# 미립 하이브리드 분리 설계서 — 직사각 경사 스크린 + 다단 원형 시브 + 터보 분급기

폐태양광 모듈 후처리 라인 — 유리 제거 후 35~500 µm 스트림에서 백시트·EVA 제거 및 구리 회수

| 항목 | 내용 |
|---|---|
| 문서 버전 | **Rev.6** (기본설계 / Basic Design) |
| 대상 설비 | SC-01 직사각 경사 스크린 · SV-01 다단 원형 시브 · CC-01 향류 분급 컬럼 · SS-01 스캘핑 시브 |
| 제작도 | `drawings/sv01-*.svg` — 총조립 · 3D · 분해 · 조립 · 정비 5매 |
| 목적함수 | **3제품(실리콘+은 / 구리 / 백시트) 회수율** — 품위는 구속조건 아님 |
| 공급물 | **35 ~ 500 µm**, 유리 제거 완료 |
| 처리량 | 정격 250 kg/h · 최대 350 kg/h |
| 계산 스크립트 | `tools/screen_sizing.py` |
| 상태 | **파일럿 검증 전 기본설계**. §10 실측으로 확정 필요 |

## 개정 이력

| Rev | 기준 | 변경 |
|---|---|---|
| 0 | 0.6~12 mm 가정, 2.0 t/h | 원형 자이라토리 Ø1800 4단 |
| 1 | 0.6~12 mm 가정, 350 kg/h | 직사각 경사 W400×L1400 2단 |
| 2 | 35~500 µm 실측, 350 kg/h | 입도 영역이 바뀌어 Rev.0~1 의 핵심 논리 두 가지가 무효가 됨(§2). 개방형 흡입 후드 폐기 → 밀폐 향류 컬럼. 분급 주역이 원형 시브로 이동 |
| **3** | **동일** | **중력식 지그재그 컬럼 폐기 → 터보(디플렉터 휠) 분급기. 밴드 상한을 폴리머가 아니라 실리콘까지 포함해 재계산(§4). 커트를 겉보기 풍속이 아닌 반경방향 풍속으로 직접 지정** |
| 3.1 | 동일 | 체 이월 민감도 추가(§6.3.1) — 허용 오차가 체와 분급기 사이에 비대칭임을 정량화. SV-01 오버사이즈 혼입률 2 % 이하를 수입 기준으로 확정 |
| 4 | 동일 · 목적함수 변경 | 품위 우선 → 회수율 우선. 제품을 3개(실리콘+은 / 구리 / 백시트)로 확정하고, 분급기를 2대 → 1대로 합쳤다. 경량측 스캘핑 시브 SS-01 신설 |
| 5 | 동일 | 체분리 거동을 불규칙 형상 입자로 수치해석(§6.8). 상단 데크 200 → 250 µm, 회수율 목표 하향 |
| **6** | **동일 · 검수 반영** | **상단 데크 250 → 280 µm(ISO) — 베드 부하 보정 유무 두 모델 모두에서 구리·백시트 최악값이 목표선 위인 유일한 개구. 6관점 독립 검수의 지적을 반영. ① 터보 분급기 폐기 — v_r 이 휠 주속을 넘어 컷 조건의 전제(동반회전)가 성립하지 않음 → 균일류 향류 컬럼 CC-01. ② 실리콘 분포를 §1 전제와 정합화 — 은 회수율 80.5 → 90.7 %. ③ 표본추출을 질량기준으로 정정. ④ 분급 라우팅을 재질 오라클에서 종말속도 물리로. ⑤ 도면 모터 위치 정정. ⑥ §6.7/§6.8 의 모순 수지 통합** |

---

## 0. Rev.6 에서 바뀐 것 — 검수가 뒤집은 것

코드·수치해석·설계를 6개 관점(유체역학 / 수치해법 / 체분리 이론 / 통계 /
설계공학 / 코드·테스트)에서 독립 검수시키고, 발견마다 반박 전담 검증을 붙였다.
살아남은 지적이 설계를 네 군데에서 바꿨다.

### 0.1 터보 분급기는 이 컷에서 성립하지 않는 기종이다 → CC-01 향류 컬럼

컷 조건(원심장 종말속도 = 반경방향 풍속)은 입자가 휠과 **동반회전**할 때만
성립한다. 그런데 이 컷은 공기가 날개를 가로지르는 속도가 휠 주속보다 빠르다.

| 회전수 | 밴드 요구 v_r | 휠 주속 ωR | ωR / v_r |
|---|---|---|---|
| 220 rpm | 3.00 m/s | 1.73 m/s | **0.58** |
| 900 rpm | 19.7 m/s | 7.07 m/s | 0.36 |
| 1,800 rpm | 46.4 m/s | 14.1 m/s | 0.30 |

**rpm 을 올리면 오히려 악화된다** — 밴드 속도가 √a ∝ ω 로 함께 오르기 때문이다.
고정 a 에서 ωR 을 키우려면 휠을 Ø2.8 m 로 키워야 한다. 결론: **75~280 µm 컷은
디플렉터 휠의 영역이 아니다.** 산업 터보 분급기가 ~100 µm 아래에서만 사는 이유다.

교체 기종은 **균일류 향류 분급 컬럼 CC-01** (Ø450, v 0.82 m/s, 정류격자) —
우리 수치해석의 UniformCell 이 정확히 이 기계다. §6.4 참조.

### 0.2 실리콘 분포가 설계서 자신의 전제와 모순이었다 — 은 회수율 80.5 → 90.7 %

§1 전제는 "실리콘·은은 75 µm 이하에 질량 95 % 이상"(실측)인데, 모델 분포
(중앙값 45 µm, GSD 1.55)는 절단 후 <75 µm 질량이 88 % 였다. **Rev.5 의
은 회수율 80.5 % 와 사양 인하(95→78 %)는 상당 부분 이 모순의 산물이었다.**
분포를 (38 µm, 1.5) 로 재보정하면(<75 µm 질량 95 %) 은 회수율은 **~90 %**(최종 280 구성 90.3 %)다.
100 % 가 못 되는 나머지는 진짜 체 거동(Gaudin·배향)이다.

### 0.3 통계 두 건 — 표본추출과 분급 라우팅

- **이중 가중 제거**: 질량기준 중앙값으로 개수기준 표본을 뽑고 다시 d³ρ 로
  가중하고 있었다. 질량기준 분포에서 직접 뽑아 입자마다 등질량 퀀텀을
  대표시키는 방식으로 정정(`sample_primaries`).
- **재질 오라클 폐기**: 회로 평가가 "구리면 중량측" 으로 분급기를 완전하다고
  가정하고 있었다. 종말속도 물리(`classify`)로 교체 — 부수 발견: 박편
  백시트의 부피등가 구경이 구리보다 더 줄어 **형상이 밴드를 넓힌다**(§6.4).

### 0.4 도면 치명 결함 — 모터가 스프링 아래에 있었다

수직축 진동모터가 방진스프링 **아래**의 앵커링된 베이스에 내장되어 있었다 —
스프링이 가진원과 데크를 절연하므로 도면대로 만들면 체가 진동하지 않는다.
모터를 스프링 위 진동 테이블 하면 체결로 정정(SWECO 형 배치).

### 0.5 상단 데크 250 → 280 µm — 베드 부하 보정이 최적점을 옮겼다

검수가 지적한 대로 통과확률 모델의 제시 횟수가 데크 부하와 무관했다.
데크별 베드 두께 ∝ 통과부하로 보정하면 상단 데크(부하 2.1배)의 통과가
느려져 250 µm 의 구리 회수율이 96.7 → 88.5 % 로 내려간다. 보정 유무는
파일럿 전에 확정할 수 없으므로 **두 모델 모두에서 최악값이 목표선
(Cu 90 / BS 95 %) 위에 있는 유일한 개구인 280 µm(ISO 표준체)** 를 채택한다.

| 상단 | 구리 회수 (보정/무보정) | 백시트 회수 (보정/무보정) |
|---|---|---|
| 250 µm | **88.5** / 96.7 % | 99.5 / 99.2 % |
| **280 µm (채택)** | **93.3 / 98.6 %** | **98.2 / 96.9 %** |
| 300 µm | 95.1 / 99.1 % | 96.7 / **94.5 %** |

### 확정 성적 (Rev.6 — 280 µm · 형상 반영 분급 · 정합 분포 · v 0.82 m/s · 보수 모델, 시드 3 평균)

| 제품 | 유량 (350 kg/h) | 회수율 (보수 ~ 낙관) |
|---|---|---|
| P1 실리콘+은 | 111.9 kg/h | **90.3 %** (± 모델 간 <1) |
| P2 구리 조립 | 33.1 kg/h | **93.4 ~ 98.6 %** |
| P3 백시트·EVA | 205.0 kg/h | **98.2 ~ 96.9 %** |

---

## 0.0 (이력) Rev.5 에서 바뀐 것 — 체 거동을 계산했더니 가정 두 개가 틀렸다

Rev.4 까지 체는 **효율 90 %** 라는 한 숫자로만 다뤘다. 실제 거동을 입자 형상까지 넣어
계산하니(§6.8) 그 가정이 두 군데서 낙관이었다.

| # | Rev.4 가정 | Rev.5 계산값 | 원인 |
|---|---|---|---|
| 1 | 구리 회수율 **99.9 %** | **82.9 %** (상단 200 µm 유지 시) | 체의 **실제 컷이 개구보다 작다.** 150~200 µm 구리가 200 µm 데크를 넘지 못하고 P3 로 유실 |
| 2 | 은 회수율 **99.0 %** | **80.5 %** *(Rev.6 에서 90.7 % 로 재정정 — §0.2)* | 데크 효율 90 % 가 낙관이고, SS-01 의 기여도 9 포인트가 아니라 **4.8 포인트** |

**대책 — 상단 데크를 250 µm 로 키운다.** 구리 회수율이 82.9 → **97.3 %** 로 회복되고
품위 손실은 98.8 → 98.2 % 로 사실상 없다. 300 µm 까지 키우면 회수율은 98.8 % 로 조금 더
오르지만 밴드가 무너져 품위가 69.9 % 로 떨어지고 백시트 회수율도 93.5 % 로 내려간다.

| 상단 데크 | 구리 회수 | 구리 품위 | 백시트 회수 |
|---|---|---|---|
| 200 µm (Rev.4) | 82.9 % | 98.8 % | 99.8 % |
| **250 µm (채택)** | **97.3 %** | **98.2 %** | **99.7 %** |
| 300 µm | 98.8 % | 69.9 % | 93.5 % |
| 355 µm | 99.0 % | 31.5 % | 67.2 % |

**은 회수율 80 % 는 되돌릴 수 없다.** 원인이 체의 성능이 아니라 **입도분포 자체**이기
때문이다 — 실리콘 질량의 11 % 가 애초에 75 µm 위에 있고, 개구에 가까운 입자는 어떤 체로도
잘 통과하지 않는다(Gaudin). 목표를 95 % 로 적어 두는 것은 달성 불가능한 사양이므로
**§10 성능목표를 80 % 로 내린다.**

> **입도분포 해석도 정정했다.** 체분석은 관례상 **질량 기준**인데 모델이 개수 기준으로
> 표본추출하고 있었다. 질량 기준으로 읽어야 "실리콘·은은 75 µm 이하에 95 % 이상" 이라는
> §1 전제와 맞는다(질량 89 % vs 개수 기준 54 %).

---

## 0.0.1 (이력) Rev.4 에서 바뀐 것 — 목적함수가 품위에서 회수율로

발주처가 요구를 확정했다. **실리콘+은 · 구리 · 백시트 3제품의 회수율**이 목적이고,
품위는 후단 공정(침출·정선)이 감당하므로 구속조건이 아니다. 은은 실리콘에서 갈라내는 것이
아니라 **결합된 상태 그대로 한 제품**으로 받는다.

이 변경으로 설계가 세 군데 달라졌다.

| # | Rev.3 | Rev.4 | 근거 |
|---|---|---|---|
| 1 | 분급기 2대 (75~106, 106~200) | **1대 (75~200 합류)** | 좁은 분획은 품위를 위한 것이었다. 회수율은 광폭 밴드에서도 99.9 % (§6.4) |
| 2 | — | **SS-01 스캘핑 시브 신설** | 체가 놓친 미분이 백시트 제품으로 나가 은이 손실된다. 되찾으면 90 → 99 % (§6.7) |
| 3 | 회전수를 분획별로 (150 / 200 rpm) | **220 rpm** | 광폭 밴드에서는 원심장이 여유비를 **넓힌다** — §6.5 의 주장이 좁은 분획에서만 참이었다 (§6.5.2) |

**데크는 줄이지 않는다.** 분급기가 하나면 106 µm 데크도 뺄 수 있을 것 같지만, 계산하면
75 µm 데크 통과부하가 168 → 239 kg/h 로 늘어 Ø1200(1.02 m²)으로는 소요면적 1.20 m² 를
감당하지 못한다. **106 µm 데크의 실제 역할은 밴드를 만드는 것이 아니라 은 회수의 병목
데크를 덜어 주는 것**이다(§6.2).

---

### 0.1 Rev.3 에서 반영한 것

(Rev.3 시점 기록 — 아래 내용은 Rev.4 에서도 유효하다.) Rev.2 §0 에 적어 둔 정정 사항 두 건을 본문에 반영했다.

| # | Rev.2 | Rev.3 |
|---|---|---|
| 1 | 지그재그 향류 컬럼, 커트 0.68 / 1.24 m/s | **터보(디플렉터 휠) 분급기 TC-01/02**, 반경방향 풍속 2.07 / 1.88 m/s (§6.4) |
| 2 | 커트를 종말속도 중점으로 지정 | 중력 컬럼은 겉보기≠국소(포물선 분포 1.5배)라 환산이 필요했으나, **터보는 휠 외주의 v_r 이 곧 설계값**이라 환산이 사라진다 |

추가로 Rev.2 계산의 오류를 하나 바로잡았다.

> **§4 의 밴드 상한을 폴리머로만 잡은 것이 틀렸다.** 75~106 µm 분획에서 실제 제약은
> **실리콘(106 µm, 0.60 m/s)** 이지 백시트(0.32 m/s)가 아니다. 이를 반영하면 이 분획의
> 여유비는 Rev.2 가 적은 3.2 가 아니라 **1.7** 이다. 106~200 µm 분획은 실리콘이 120 µm
> 까지만 존재하므로 제약이 백시트(200 µm)로 남아 여유비 2.0 이 유지된다.

---

## 1. 설계 전제

| 항목 | 값 | 출처 |
|---|---|---|
| 공급량 | 정격 250 / 최대 350 kg/h | 실측 |
| 입도 범위 | 35 ~ 500 µm | 실측 |
| 구리 | **75~200 µm 에 99 % 이상** | 실측 |
| 백시트(EVA 부착) | **75~500 µm 에 분포** | 실측 |
| 실리콘 · 은 | **75 µm 이하에 95 % 이상** | 실측 |
| 유리 | 없음 (전단에서 제거) | 실측 |

**밀도 (가정)**

| 물질 | 밀도 |
|---|---|
| 구리 | 8,960 kg/m³ |
| 실리콘 (+Ag) | 2,500 kg/m³ |
| 백시트 + EVA | 1,200 kg/m³ |
| EVA | 950 kg/m³ |

**입도 분획별 중량비 (가정 — 체분석으로 반드시 검증)**

| 분획 | 중량비 | 주 성분 |
|---|---|---|
| 200 ~ 500 µm | 31.7 % | 백시트 · EVA (구리 없음) |
| 106 ~ 200 µm | 20.2 % | 구리 + 폴리머 |
| 75 ~ 106 µm | 12.7 % | 구리 + 폴리머 |
| < 75 µm | 35.4 % | **실리콘 + 은** |

---

## 2. Rev.0~1 논리 중 무효가 된 것

입도 영역이 바뀌면서 앞선 설계의 근거 두 가지가 깨진다. 먼저 짚고 간다.

### 2.1 "종말속도 = 두께 × 밀도, 크기 무관" 은 여기서 성립하지 않는다

백시트 필름 두께는 약 300 µm 다. 입자가 35~500 µm 라면 그 입자는 **더 이상 얇은 박편이 아니라
거의 등방(等方) 덩어리**다. 판상 가정이 무너지므로 종말속도는 크기와 밀도 **둘 다**에 지배된다.

### 2.2 개방형 흡입 후드는 이 물질에 쓸 수 없다

| 후드 면속도 | 딸려 올라가는 구리 |
|---|---|
| 2.0 m/s | 122 µm 이하 |
| 2.5 m/s | 148 µm 이하 |
| **2.8 m/s** (Rev.1 설계값) | **163 µm 이하** |
| 3.2 m/s | 184 µm 이하 |

**구리의 99 % 가 75~200 µm 에 있으므로 Rev.1 후드를 그대로 쓰면 구리를 사실상 전량 날린다.**

중력장에서 필요한 커트 속도는 **0.8 ~ 1.2 m/s**(§4)인데, 개방형 후드는 실내 기류에 지지
않으려면 최소 2 m/s 가 필요하다. **원리적으로 양립 불가** → 후드를 버린다.
(Rev.2 는 대안으로 밀폐 향류 컬럼을 두었으나, 그 기종도 이 입도에서는 범위 밖이라
Rev.3 에서 **터보 분급기**로 바뀌었다 — §6.4.)

---

## 3. 종말속도

| 물질 | 35 µm | 50 | 75 | 106 | 150 | 200 | 300 | 500 µm |
|---|---|---|---|---|---|---|---|---|
| 구리 | 0.296 | 0.550 | **1.037** | 1.670 | 2.548 | **3.490** | 5.220 | 8.260 |
| 실리콘 (+Ag) | 0.088 | 0.171 | 0.347 | 0.551 | 0.985 | 1.416 | 2.232 | 3.694 |
| 백시트+EVA | 0.043 | 0.085 | **0.178** | 0.321 | 0.548 | **0.816** | 1.337 | 2.290 |
| EVA | 0.035 | 0.068 | 0.144 | 0.267 | 0.452 | 0.680 | 1.131 | 1.961 |

단위 m/s. 구리 200 µm 의 Re ≈ 46 으로 Stokes 영역을 벗어나 있어, 전이영역 항력계수로 수렴 계산했다.

---

## 4. 핵심 — 등침강 입경비가 분획 폭의 상한을 정한다

에어분급은 **밀도가 아니라 종말속도**로 가른다. 큰 경량 입자와 작은 중량 입자는 같은 속도로
떨어진다. 구리와 같은 속도로 떨어지는 폴리머 입경은 다음과 같다.

| 구리 입경 | 같은 속도의 백시트+EVA | 입경비 | 같은 속도의 EVA | 입경비 |
|---|---|---|---|---|
| 75 µm | 242 µm | **3.22** | 279 µm | 3.72 |
| 100 µm | 342 µm | 3.42 | 398 µm | 3.98 |
| 150 µm | 558 µm | 3.72 | 655 µm | 4.37 |
| 200 µm | 785 µm | 3.93 | 927 µm | 4.64 |

> **입도 분획을 3.2 : 1 보다 좁게 잘라야 밀도로 갈린다.** 이것이 다단 시브가 필요한 이유이며,
> 분급을 안 하고 통째로 흡입하면 어떤 풍속을 골라도 구리와 폴리머가 섞여 나온다.

### 분획별 검토 — 밴드 상한은 실리콘까지 포함해서 잡는다

Rev.2 는 상한을 폴리머로만 잡아 75~106 µm 분획의 여유비를 3.2 로 과대평가했다.
**이 분획의 실제 제약은 실리콘이다.**

| 분획 | 구리 하한 | 비구리 상한 | 제약 재질 | 여유비 |
|---|---|---|---|---|
| 75 ~ 106 µm | 1.037 m/s (Cu 75 µm) | **0.602 m/s** | **실리콘 106 µm** | **1.72** |
| 106 ~ 200 µm | 1.670 m/s (Cu 106 µm) | 0.816 m/s | 백시트 200 µm | 2.05 |

106~200 µm 에서는 실리콘이 120 µm 까지만 존재하므로 제약이 백시트로 남는다.
분획을 3.2 : 1 보다 좁게 잘라야 한다는 §4 의 원칙 자체는 그대로다 — 다만
**여유가 Rev.2 가 생각한 것보다 좁다.**

---

## 5. 응집 한계 — 어디까지 건식으로 가능한가

표면조도 보정 Bond 수(부착력 / 자중). 1 을 넘으면 응집이 중력을 이긴다.

| 입경 | Bo | 판정 |
|---|---|---|
| 20 µm | 131.8 | 응집 지배 — 건식 분급 불가 |
| 35 µm | 24.6 | 응집 지배 — 건식 분급 불가 |
| 50 µm | 8.44 | 응집 지배 — 건식 분급 불가 |
| **75 µm** | **2.50** | 응집 우세 — 강제 분산 필수 |
| 106 µm | 0.89 | 경계 — 분산기 병용 시 가능 |
| 150 µm | 0.31 | 경계 — 분산기 병용 시 가능 |
| 200 µm | 0.13 | 자중 지배 — 분급 가능 |

차수 추정이지만 Geldart 분류(<30 µm = C 응집군, 30~100 = A, 100~800 = B)와 같은 결론이다.
**EVA 는 연질·점착성이라 vdW 외에 소성접촉 부착이 더해지므로 실제 한계는 위 표보다 나쁘다.**

> **결론 — < 75 µm 분획(실리콘 + 은)은 건식 에어분급 대상이 아니다.** 시브로 분리해 내는 데까지가
> 건식 라인의 역할이고, 그 안의 정선은 습식 또는 정전선별 영역이다.

---

## 6. 공정 구성 — 하이브리드에서 각 기계의 실제 역할

35~500 µm 에서 필요한 컷은 200 / 106 / 75 µm 인데, **직사각 경사 진동 스크린은 이 중 어느 것도
낼 수 없다**(실효 하한 약 0.5~1 mm). 따라서 하이브리드의 무게중심이 원형 시브로 완전히 넘어간다.

```
 분쇄 산물 (35~500 µm)
     │
 [SC-01] 직사각 경사 스크린 0.9 mm  ── O/S ─► 응집체·이물 배출 / 해쇄 후 재공급
     │  ※ 역할: 스캘핑 + 해쇄 + 정량 분산 공급 (분급 아님)
     ▼
 [SV-01] 다단 원형 시브 Ø1200, 초음파 3단
     ├─ 280 µm O/S ── 280~500 µm ──────────────────────────────────────┐
     ├─ 106 µm O/S ── 106~280 µm ──┐                                   │
     ├─  75 µm O/S ──  75~106 µm ──┴─ 합류 ─► [CC-01] 향류 컬럼       │
     │                                        Ø450 · v 0.82 m/s       │
     │                                              │                  │
     │                                    중량 ─────┴──── 경량         │
     │                                     │               │           │
     │                                     ▼               ▼           │
     │                              구리 조립 31.9      [SS-01] 스캘핑  │
     │                                     │            Ø900 75 µm     │
     │                                     │           초음파 단단     │
     │                                     │             │      │      │
     │                                     │          U/S│      │O/S   │
     └─ PAN ── < 75 µm ──────────────┐     │             │      └──────┤
                                     ▼     │             ▼             ▼
                        ┌─────────────────┐│  ┌────────────────┐ ┌───────────┐
                        │ P1 실리콘+은     ││  │ (P1 로 합류)   │ │ P3 백시트 │
                        │  111.9 kg/h     │◄┼──┘                │ 205.0 kg/h│
                        └─────────────────┘│                    └───────────┘
                                           ▼
                                  ┌──────────────┐
                                  │ P2 구리 조립 │──► [ES-01] 정전/습식 정선 ─► 구리 정광
                                  │  33.1 kg/h   │
                                  └──────────────┘
```

*(유량은 최대 350 kg/h · 75 µm 데크 효율 90 % 기준 — §6.7)*

**< 75 µm 는 에어분급 금지**(§5). 실리콘과 은은 결합 상태 그대로 P1 으로 간다 —
갈라내는 것이 물리적으로 불가능하기 때문이다(§8).

### 6.1 SC-01 직사각 경사 스크린 — 조연

| 항목 | 사양 |
|---|---|
| 역할 | 0.9 mm 스캘핑(응집체·이물 제거) + 해쇄 + 정량 분산 공급 |
| 데크 | W 400 × L 1,000 mm, 1단 |
| 경사각 | 12°, 직선운동, 스트로크 3~4 mm |
| 구동 | 불평형 진동모터 0.4 kW × 2 |
| 커버 | 전면 밀폐 |

이 기계는 **분급을 하지 않는다.** 점착성 EVA 때문에 반드시 생기는 응집체를 걷어내고 고가의
미세 데크를 보호하는 것이 목적이다. 없어도 공정은 돌지만 SV-01 의 눈막힘 주기가 짧아진다.

### 6.2 SV-01 다단 원형 시브 — 주연

| 데크 | 통과부하 (최대) | 기준능력 | 소요면적 |
|---|---|---|---|
| **280 µm** | 350 kg/h | 0.50 t/h/m² | 0.70 m² |
| 106 µm | 239 kg/h | 0.30 t/h/m² | 0.80 m² |
| **75 µm** | **168 kg/h** | 0.20 t/h/m² | **0.84 m²** ← 지배 |

**상단 개구는 200 이 아니라 280 µm 다**(§0.5·§6.8) — 200 은 근접입자 구리를 유실하고,
250 은 베드 부하 보정 모델에서 구리 88.5 % 로 목표 미달, 300 은 백시트를 잃는다.

> **면적 여유 주의** — 280 채택으로 106 µm 데크 통과부하가 280 kg/h 로 늘어
> 지배 데크가 되며(소요 0.933 m²), Ø1200 여유는 **+9 %** 로 얇아진다.
> 파일럿에서 데크 능력 가정(0.30 t/h/m²)이 5 % 만 낙관이어도 Ø1500 으로 가야 한다.

**106 µm 데크는 분급기가 하나가 되어도 남긴다.** Rev.4 에서 두 분획을 합류시키므로 이 데크가
만드는 컷은 더 이상 밴드에 쓰이지 않는다. 그래도 빼지 않는 이유는 부하 때문이다.

| 구성 | 75 µm 데크 통과부하 | 소요면적 | Ø1200(1.02 m²) 여유 |
|---|---|---|---|
| **3메쉬 유지 (200/106/75)** | 168 kg/h | 0.84 m² | **+21 %** |
| 106 데크 제거 (200/75) | 239 kg/h | 1.20 m² | **−15 % — 불가** |

데크는 위에서부터 걸러지므로, 106 데크가 빠지면 106~200 µm 분(20.2 %)이 그대로 75 µm
데크 위를 지나간다. 부하가 **1.42 배**로 뛰고 Ø1500 으로 키워야 한다. **106 µm 데크의 실제
역할은 밴드 생성이 아니라 은 회수의 병목 데크를 덜어 주는 것**이며, 그 값어치는 Ø1200 과
Ø1500 의 차액이다.

| 직경 | 유효면적 | 여유 | 판정 |
|---|---|---|---|
| Ø1000 | 0.71 m² | −16 % | 불가 |
| **Ø1200** | **1.02 m²** | **+21 %** | **채택** |
| Ø1500 | 1.59 m² | +89 % | 파일럿 결과가 나쁘면 |

| 항목 | 사양 |
|---|---|
| 형식 | 자이라토리형 원형 시브 |
| 직경 / 데크 | Ø1200 / 4단 = 메쉬 3단 (**250** / 106 / 75 µm) + 팬 |
| **초음파** | **35~40 kHz, 3단 전부 적용** — 선택 아님 (§6.3) |
| 메쉬 | STS316 평직, 백업메쉬 병용 |
| 구동 | 2.2 kW |
| 커버 | 전면 밀폐, 미압 부압 |

### 6.3 초음파가 여기서는 은 회수율을 직접 결정한다

| 재질 | 경량측으로 가는 최대 입경 (CC-01, v 0.82 m/s · 중력장 · 구형 기준) | 실제 거동 |
|---|---|---|
| 실리콘(+Ag) | **131 µm 이하** | 존재범위 ~120 µm → **전량 경량측** |
| 백시트+EVA | 200 µm 이하 (구형) | **형상 반영 시 ~280 µm 까지 경량측** — §6.4 게이트 |
| 구리 | 64 µm 이하 | 존재범위 75 µm 부터 → **전량 중량측** |

**75 µm 체를 빠져나가지 못한 미분 실리콘은 그대로 은 손실이 된다.** 커트 속도가 낮아
실리콘이 예외 없이 경량(폐기)측으로 가기 때문이다. 즉 **75 µm 데크의 체 효율 = 은 회수율**이며,
눈막힘이 진행되면 은이 폴리머와 함께 버려진다. 볼 데크로는 이 영역을 지킬 수 없다.

### 6.3.1 체 이월(oversize leak) — 정밀도가 필요한 쪽은 분급기가 아니라 체다

> **Rev.6 주** — 아래 표는 Rev.4/5 의 터보 운전점(75~200 µm, v_r 2.66)에서 구형
> 가정으로 계산된 값이다. 기종이 컬럼으로 바뀌고 밴드가 형상 의존이 되면서
> 절대값은 이력이 되었지만, **정성 결론 — 이월·응집은 품위를 깎고 회수율은
> 안 깎는다, 체 정밀도가 분급 정밀도보다 비싸다 — 는 기종과 무관하게 유효하다.**
> Rev.6 운전점의 회수율 민감도는 §6.4 의 v_cut 스윕이 담당한다.

§6.4.1 에서 "체가 입도를 좁혀 주기 때문에 분급기가 밀도를 볼 수 있다"고 했다. 그렇다면
**체가 좁히다 만 경우** 성능이 얼마나 무너지는지가 곧 SV-01 의 사양을 정한다.

체눈 파손·눈막힘·응집체가 포락 입경으로 체질되는 경우는 모두 같은 형태로 나타난다 —
**분획 상한을 넘는 폴리머가 분급기 공급물에 섞여 들어온다.** 150~250 µm 백시트가
Rev.4 의 75~200 µm 분획(TC-01, v_r 2.66 m/s)에 이월된 비율을 바꿔가며 계산한 결과다.

| 이월 질량비 | 완전분산 회수 / 품위 | 분산효율 85 % 회수 / 품위 |
|---|---|---|
| 0 % | 100.0 / **100.0** | 99.9 / 98.6 |
| 1 % | 100.0 / **99.2** | 99.8 / 97.8 |
| 2 % | 100.0 / **99.0** | 99.8 / 97.3 |
| 5 % | 100.0 / **97.5** | 99.7 / 95.8 |
| 10 % | 100.0 / **94.5** | 100.0 / 92.5 |
| 20 % | 100.0 / **89.2** | 99.9 / 87.4 |

`tools/classifier_sim.py` 의 `evaluate_feed(..., sieve_leak=)` (기본 시드) 로 재현된다.

**이월분은 거의 그대로 품위 손실이 된다.** 회수율은 전혀 흔들리지 않는다 —
응집(§6.6)과 같은 양상이다.

> **광폭 밴드는 이월에 더 강하다.** Rev.3 의 좁은 분획(75~106)에서는 이월 10 % 가 품위를
> 90.1 % 까지 떨어뜨렸는데, Rev.4 의 75~200 에서는 94.5 % 에 머문다. 넘어온 150~250 µm
> 입자가 **밴드 기준으로 덜 "초과"** 이기 때문이다. 분급기를 합친 것이 이월 내성 면에서는
> 이득이었다. 이월된 굵은 백시트는
자신의 종말속도가 커트보다 커서 **중량측으로 곧장 간다**. 구리와 같은 출구로 나가므로
분급기가 손쓸 방법이 없다.

#### 같은 크기의 오차를 분급기 쪽에 주면

TC-01 운전점 2.66 m/s 를 일부러 틀어 보면:

| v_r 이탈 | 실제 v_r | 회수 / 품위 |
|---|---|---|
| −20 % | 2.13 m/s | 100.0 / 94.3 |
| −15 % | 2.26 m/s | 100.0 / 99.4 |
| −10 % | 2.40 m/s | 100.0 / **100.0** |
| ±0 % | 2.66 m/s | 100.0 / **100.0** |
| +15 % | 3.06 m/s | 100.0 / **100.0** |
| +20 % | 3.20 m/s | 99.9 / **100.0** |

**−10 % 에서 +15 % 까지 품위 손실이 0 이다.** 이 폭은 우연이 아니라 §4 의 분리 밴드
2.29~3.10 m/s 그 자체다 — 2.40 은 밴드 안이고 2.13 은 밖이라 백시트가 중량측으로 넘어온다.
수치 시뮬레이션이 해석적 밴드 경계를 그대로 재현한다.

**그리고 회수율은 어느 경우에도 무너지지 않는다** — 밴드 밖으로 20 % 벗어나도 99.9 % 다.
Rev.4 의 목적함수에서는 이 표 전체가 무손실 영역이다.

> **결론 — 허용 오차의 비대칭.** 분급기는 밴드 밖으로 나가도 회수율이 온전하고,
> 체는 2 % 만 새도 품위 2 포인트를 잃는다. 성능을 만드는 것은 분급기의 정밀도가 아니라
> **체가 만든 입도 창의 무결성**이다.
>
> 따라서 **SV-01 은 원가절감 대상이 아니다.** 데크 장력, 초음파 출력, 체눈 검사 주기가
> 분급기 회전수 정밀도보다 우선한다. §10 파일럿에서 각 분획의 오버사이즈 혼입률을
> 실측하고, **2 % 이하**를 수입 기준으로 삼는다.

### 6.4 CC-01 균일류 향류 분급 컬럼 — 1대

> **Rev.6 — 터보(디플렉터 휠)를 폐기했다.** 컷 조건 `원심장 종말속도 = v_r` 은
> 입자가 휠과 동반회전할 때만 성립하는데, 이 컷은 v_r(3.0 m/s)이 휠 주속
> ωR(1.73 m/s)을 넘는다. 공기가 날개 사이를 곧장 가로지르므로 휠은 분급기가
> 아니라 조잡한 충돌기가 된다. rpm 을 올리면 밴드 속도가 √a 로 함께 올라
> 비율이 **더 나빠진다**(§0.1 표). 75~280 µm 컷은 이 기종의 영역 밖이다.

**채택 — 균일류 향류 분급 컬럼.** 수직 컬럼에 정류격자(허니컴)를 얹어 균일
상승류를 만들고, 종말속도가 풍속보다 큰 입자(구리)는 가라앉아 하부 로터리
밸브로, 작은 입자(폴리머·미분)는 상부로 넘어간다. **우리 수치해석의
UniformCell 이 정확히 이 기계다** — 모델과 기계가 처음으로 일치한다.

| 항목 | 사양 |
|---|---|
| 형식 | 수직 향류(elutriation) 컬럼, 정류격자 + 다공 분산판 |
| 컷 | **v 0.82 m/s** (겉보기 = 국소 — 난류 평탄 프로파일, Re_D ≈ 24,000) |
| 컬럼 | **Ø450 mm**, 공급 ~150 kg/h (75~280 µm + 체 이월분) |
| 풍량 | 474 m³/h · 고형물 부하 0.30 kg/m³ |
| 제어 | 유량계 기반 폐루프 (백필터 부하 변동 보상) |

**운전점 v 0.82 m/s 는 표본 위 직접 스윕으로 골랐다** (회수율 우선 — 구리부터
지키고 백시트 혼입은 다음):

| v_cut | 구리 회수 | 백시트→P2 혼입 | 백시트 회수 |
|---|---|---|---|
| 0.70 | 97.2 % | 3.4 % | 96.6 % |
| **0.82** | **97.0 %** | **1.3 %** | **98.7 %** |
| 0.92 | 96.0 % | 0.2 % | 99.8 % |
| 1.00 | 94.5 % | 0.0 % | 100.0 % |

±12 % 이탈에 구리 회수율이 0.5 포인트밖에 안 움직인다 — **회수율 관점의
운전창은 넓다.** (품위 관점은 §6.3.1 의 Rev.6 주 참조.)

#### 이 설계는 입자 형상에 걸려 있다 — §10 의 결정 게이트

체는 중간축 I 를 재고, 분급은 부피등가 구경 d_eq = I·(elong·flat)^⅓ 로
가라앉는다. 박편 백시트(flat 0.35)는 d_eq 가 I 의 0.79 배까지 줄어드는 반면
구리 리본(flat 0.50)은 0.94 배다 — **형상이 백시트를 상대적으로 가볍게 만들어
밴드를 연다.**

| 가정 | 밴드 (1g) | 여유비 | 판정 |
|---|---|---|---|
| 구형 | 1.08 ~ 1.04 m/s | **0.96** | 닫힘 |
| **형상 반영** | 0.81 ~ 0.95 m/s | **1.17** | 열림 |

3축 비는 이 해석에서 **가장 불확실한 입력**이므로(§6.8), §10 의 형상 실측
(4-3)이 이 기종 선정의 결정 게이트다. **실측이 구형에 가깝게 나오면 대안은
진동 에어테이블** — 케이블 재활용에서 같은 duty(구리/폴리머 granulate)에
쓰이는 산업 표준 기종이며, 밀도 성층 원리라 형상 민감도가 낮다. §10 에
컬럼 vs 에어테이블 병렬 시험(4-6)을 추가했다.

### 6.4.1 왜 미세 컷을 분급기가 아니라 체로 내는가

분급기로 200/106/75 µm 컷을 대신할 수 있지 않을까 — 답은 **아니다**.

**체는 기하학적 입도로, 분급기는 종말속도로 자른다.** 종말속도는 밀도에 의존하므로
분급기의 "컷 입경"은 재질마다 다르다. 같은 설정(200 rpm)에서:

| 커트를 맞춘 기준 | v_r | 구리가 잘리는 입경 | 실리콘 | 백시트 | EVA |
|---|---|---|---|---|---|
| 백시트 106 µm | 0.89 m/s | **35.0 µm** | 70.2 | 106.0 | 121.1 |
| 구리 106 µm | 4.06 m/s | 106.0 | 242.6 | **398.9** | 468.8 |

백시트를 106 µm 에서 자르는 설정이 구리는 35 µm 에서 자른다. 즉 **§4 가 요구하는
"입도 분획을 3.2 : 1 보다 좁게" 라는 조건 자체를 분급기로는 만들 수 없다.**

**직렬로 놓아도 소용없다.** 속도로 자른 뒤 다시 속도로 자르는 것은 새로운 판별축을
만들지 못한다 — 1단 v1, 2단 v2 (v2 > v1) 의 순 결과는 v2 단독 컷과 같다.
반복 분급은 같은 컷을 예리하게 할 뿐이다(지그재그의 원리).

| 회로 | 구리 회수율 | 구리 품위 |
|---|---|---|
| 분급기만 (전 입도 35~500 µm 를 한 번에) — 최적점 | 88.0 % | **42.1 %** |
| 분급기 직렬 2단 (v1 = 2.0, v2 = 4.0) | 90.9 % | 31.1 % |
| **체 + 분급기 (Rev.3)** — TC-01 | ~100 % | **~100 %** |

> **체(기하 입도)와 분급기(종말속도)는 서로 독립인 두 측정이고, 그 독립성이 밀도 판별을
> 가능하게 한다.** 체를 빼면 밀도 판별의 근거 자체가 사라진다. SV-01 은 비용 절감 대상이 아니다.

---

### 6.5 (이력) 원심장으로 바꿔서 얻는 것과 잃는 것

> **Rev.6 주** — 이 절과 §6.5.1~6.5.2 는 Rev.3~5 의 판단 경위다. Rev.6 에서
> 터보 자체가 무효가 되어(§0.1) 원심장 논의는 이력이 되었지만, §6.5.2 의
> 임계 분획폭 유도는 물리로서 유효하다 — 원심장을 쓸 수 **있었다면** 광폭
> 밴드에 유리했을 것이라는 결론까지 포함해서. 못 쓰는 이유가 §0.1 이다.

> ⚠ **좁은 분획에서는 원심장이 분리 여유비를 넓혀 주지 않는다.** 가속도를 올리면
> 구리·폴리머 속도가 함께 오르고, 항력이 Stokes 에서 Newton 쪽으로 밀리면서 여유비는 오히려
> **미세하게 좁아진다** (75~106 µm: 1g 에서 1.72 → 13g 에서 1.67). 예리도(d25/d75)도
> 수치해석에서 0.84 → 0.81~0.82 로 개선되지 않았다.
>
> **단, 이것은 분획이 좁을 때의 이야기다.** Rev.3 은 이를 무조건적 성질로 적었는데
> 틀렸다 — 임계 분획폭을 넘으면 부호가 뒤집힌다(§6.5.2). Rev.4 의 광폭 밴드가 그 영역이다.

**그래도 터보로 가는 이유**

| # | 이유 |
|---|---|
| 1 | **중력식 지그재그가 이 입도에서 작동 범위 밖이다** (실용 0.3~10 mm). 이것만으로 충분한 이유다 |
| 2 | **난류 여유가 2.6 배 넓다** — 터보의 밴드 폭이 절대값으로 1.11 m/s 로 중력식(0.44)보다 넓다. 다만 §6.5.1 의 정정대로 **실제 기계 난류 영역에서는 둘의 성능 차이가 없다** |
| 3 | **설계값이 곧 운전값이다.** 중력 컬럼은 포물선 분포 때문에 겉보기 풍속이 국소 속도의 1/1.5 라 환산이 필요했지만, 터보는 휠 외주의 v_r 을 직접 지정한다 |
| 4 | **노브가 둘이다.** 회전수(a)와 풍량(v_r)을 독립으로 조절해 컷을 옮길 수 있다. 컬럼은 풍속 하나뿐 |

**대가** — 없다. Rev.2 대비 풍량이 208 → 436 m³/h 로 늘었지만 **이것은 기종 때문이 아니다**
(§6.5.1).

**운전 조건**

- 고형물 부하 **≤ 0.5 kg/m³** 유지 — 넘으면 예리도가 급격히 나빠진다(위 표는 0.20~0.34)
- 휠 입구에 **강제 분산기** — Bo ≈ 0.9~2.5 구간이라 응집 상태로 들어오면 분급이 성립하지 않는다
- 회전수·풍량 각각 **인버터 + 폐루프 제어**. Rev.4 의 여유비는 1.36 으로 Rev.3(1.70)보다
  얇다. 수치해석상 무손실 폭은 **−10 % ~ +15 %** 이므로 실운전은 **±5 % 이내**로 잡는다.
  드리프트의 주원인은 회전수가 아니라 **백필터 부하에 따른 풍량 변동**이므로,
  댐퍼가 아니라 유량계 기반 폐루프가 필요하다
- 습도 **40 ~ 50 % RH**
- 휠·케이싱 내마모 처리 — 실리콘·유리 잔사가 연마성이다

---

### 6.5.1 정정 — Rev.3 초안에서 내가 틀리게 쓴 두 가지

균일 유동 셀로 중력식과 터보를 **같은 조건에서** 다시 비교한 결과, §6.5 에 적었던 근거 두
가지가 사실과 달랐다.

**① "터보가 난류에 견고하다"는 과장이었다.**

앞선 비교는 중력식을 `ZigZagColumn`(포물선 분포)에서 겉보기 풍속 1.20 m/s 로 돌린 것인데,
그 국소 중심속도가 1.5 × 1.20 = 1.80 m/s 로 **구리 하한(1.04)을 이미 넘고 있었다.**
밴드 밖에서 운전한 결과를 "중력식의 성질"로 읽은 것이다. 균일 셀에서 각자 밴드
기하평균으로 맞춰 비교하면:

| 난류 σ [m/s] | σ / 밴드폭 (중력) | 중력 품위 | σ / 밴드폭 (터보) | 터보 품위 |
|---|---|---|---|---|
| 0.5 | 1.15 | 100 % | 0.45 | 100 % |
| 1.0 | 2.30 | 100 % | 0.90 | 100 % |
| 2.0 | 4.60 | 91.8 % | 1.79 | 99.9 % |
| 3.0 | 6.89 | 65.9 % | 2.69 | 96.0 % |

터보가 더 견고한 것은 맞다 — 밴드 폭이 절대값으로 2.6 배 넓기 때문이다. 그러나 그 이점은
**σ > 2 m/s 라는 비현실적 영역에서만 나타난다.** 실제 기계 난류(σ ≈ 0.2 m/s)에서는 둘 다 100 % 다.
난류는 에디 수명(20 ms) 위에서 평균되어 편향이 아니라 확산으로 작용하므로, 체류시간이
충분하면 평균 표류의 부호가 유지된다.

**② 풍량 증가는 기종 때문이 아니라 고형물 부하를 바로잡은 결과다.**

풍량은 `Q = 고형물 / 부하` 로 정해지므로 **기종과 무관**하다.

| 구성 | 고형물 | 부하 | 풍량 |
|---|---|---|---|
| Rev.2 AC-01 (지그재그) | 44.5 kg/h | 0.61 kg/m³ | 73 m³/h |
| Rev.3 TC-01 (터보) | 44.5 kg/h | 0.195 kg/m³ | 228 m³/h |
| 중력식을 부하 0.20 으로 운전하면 | 44.5 kg/h | 0.20 kg/m³ | **222 m³/h** |

Rev.2 의 208 m³/h 는 부하 0.53~0.61 kg/m³ 로 운전한 값인데, 이는 뒤에 내가 세운
한계(≤ 0.5)를 이미 넘고 있었다. **같은 부하로 맞추면 중력식이든 터보든 풍량은 같다.**

**정정 후에도 결론은 바뀌지 않는다** — 다만 이유가 달라진다. 분리 성능으로는 두 기종을
고를 수 없고(여유비 1.72 vs 1.70, 실제 난류에서 둘 다 100 %), **기종 가용성과 제어성**이
결정한다: 지그재그는 이 입도가 작동 범위 밖(0.3~10 mm)이고, 터보는 설계값이 곧 운전값이며
회전수·풍량 두 노브로 컷을 옮길 수 있다.

---

### 6.5.2 정정 — "원심장은 여유비를 넓히지 않는다" 는 좁은 분획에서만 참이다

Rev.3 §6.5 는 이것을 기종의 일반 성질처럼 적었다. 광폭 밴드를 검토하면서 다시 계산하니
**분획 폭에 따라 부호가 뒤집힌다.**

| 분획 | 폭 (hi/lo) | a/g 1 | a/g 4 | a/g 16 | 방향 |
|---|---|---|---|---|---|
| 75~106 µm | 1.41 | 1.723 | 1.692 | 1.668 | 좁아짐 |
| 75~150 µm | 2.00 | 1.435 | 1.449 | 1.460 | 넓어짐 |
| **75~200 µm** | **2.67** | **1.272** | **1.357** | **1.425** | **넓어짐** |

**이유는 항력 지수다.** Stokes 에서는 `v ∝ ρd²`, Newton 에서는 `v ∝ √(ρd)` 다. 가속도를
올리면 Re 가 커져 Newton 쪽으로 밀리는데, 이때 **입경의 지수가 2 → ½ 로 떨어진다.**
즉 원심장은 밀도 대비 **크기의 영향을 줄인다.**

- 분획이 좁으면 크기 차가 작아 애초에 방해가 안 되므로, 크기 영향을 줄여 봐야 얻을 게 없고
  밀도비의 지수까지 1 → ½ 로 깎이는 손해만 남는다.
- 분획이 넓으면 크기 차가 주된 방해이므로, 그것을 눌러 주는 이득이 손해를 넘는다.

**임계 분획폭은 밀도비의 세제곱근이다.**

```
Stokes  여유비 = ρ_r / sr²        Newton 여유비 = √(ρ_r / sr)
두 값이 같아지는 지점:  sr = ρ_r^(1/3)
```

| 재질쌍 | 밀도비 | 임계 분획폭 |
|---|---|---|
| 구리 / 백시트+EVA | 7.47 | **1.96 : 1** |
| 구리 / 실리콘(+Ag) | 3.58 | **1.53 : 1** |

수치로 확인하면 임계폭(1.96)에서 a/g 1 → 16 의 변화량이 −0.0002 로 정확히 0 을 지나고,
1.66 에서는 −0.163, 2.25 에서는 +0.089 다. `tests/test_screen_sizing.py::BandWidthTest`
가 이 기준을 고정한다.

> **Rev.4 가 220 rpm 을 쓰는 이유가 이것이다.** 75~200 µm(폭 2.67)는 임계폭을 넘으므로
> 회전수를 올릴수록 여유비가 넓어진다. 150 rpm 의 1.31 대신 220 rpm 의 **1.36** 을 쓴다.
> 더 올려도 1.39 로 수렴하고 휠이 과도하게 작아져 공급 균일도가 나빠지므로 220 에서 멈춘다.

---

### 6.6 응집 보정 — 이상 모델의 낙관 편향을 걷어낸다

> **Rev.6 주** — 수치는 터보 운전점 기준 이력이다(§6.3.1 주와 동일). 분산기
> 요구(응집체가 체를 막으면 은 회수가 무너진다)는 기종과 무관하게 유지된다.

지금까지의 수치해석은 입자를 서로 독립인 구로 두어 밴드 안이면 회수·품위가 모두 100 % 로
나왔다. 실제로는 Bo ≈ 1 부근에서 부착력이 자중을 이겨 **응집체**가 생긴다. 응집체는
구성 입자들의 **평균 밀도**로 거동하므로, 구리가 폴리머 응집체에 섞이면 그 폴리머를
중량측으로 끌고 들어간다.

#### 모델

| 항목 | 처리 |
|---|---|
| 부착력 | 표면조도 보정 `F = A_H·r_asp / (6 z²)` — 입경 무관. 재질별 점착 배수(EVA ×3, 백시트 ×2) |
| 부착 확률 | `Bo/(1+Bo) × (1 − 분산기 효율)` |
| 응집체 형상 | 프랙탈 차원 `D_f = 2.4`. `d_eff = d_solid · N^(1/D_f − 1/3)`, `ρ_eff = ρ_mass · N^(1 − 3/D_f)` |
| 성적 집계 | 응집체 단위로 분리하되 **1차 입자 질량 기준**으로 집계 — 갇힌 구리는 회수 실패로 계산 |

**입경 100 µm 에서 Bo** — 구리 0.29(잘 안 붙는다) / 백시트 4.4 / EVA 8.3 / 실리콘(60 µm) 4.9.
구리만 자중이 이기고 나머지는 부착이 이긴다.

#### 결과 — 응집은 회수율이 아니라 품위를 무너뜨린다

Rev.4 의 단일 분급기(75~200 µm, v_r 2.66 m/s) 기준으로 다시 계산했다.

| 분산기 효율 | 구리 품위 | 구리 회수율 |
|---|---|---|
| 100 % (이상) | 100.0 % | 100.0 % |
| 95 % | 99.5 % | 99.9 % |
| **85 %** | **98.6 %** | **99.9 %** |
| 70 % | 96.9 % | 99.6 % |
| 50 % | 93.6 % | 99.5 % |
| 0 % | **83.6 %** | 99.2 % |

구리는 Bo 가 0.29 로 낮아 응집체에 잘 끼지 않는다 → **회수율은 거의 그대로**.
반면 구리가 낀 응집체는 폴리머를 함께 중량측으로 끌고 가므로 **품위가 무너진다**.

#### 설계 요구사항 — 분산기 효율

| 품위 목표 | 소요 분산기 효율 |
|---|---|
| 95 % | 55 % |
| 97 % | 70 % |
| **98 %** | **79 %** |
| 99 % | 90 % |

> **Rev.4 주의 — 이 표의 위상이 달라졌다.** 품위가 구속조건이 아니므로 분산기 효율은
> 더 이상 성능을 좌우하지 않는다. **회수율은 분산 0 % 에서도 99.2 %** 로 유지된다.
> 분산기는 여전히 필요하지만(응집체가 체를 막으면 은 회수가 무너진다 — §6.3),
> 그 근거는 이제 분급기 품위가 아니라 **체 효율**이다.

> **분산기 효율 ≥ 85 % 를 설계 요구사항으로 확정한다.** 이 값이 구리 품위를 직접 결정하며,
> 기계 사양(회전수·풍량)보다 성능에 더 크게 기여한다. §10 파일럿에서 실측해야 할 1순위다.

##### 모델의 한계 — 여전히 낙관 쪽이다

- **응집체 내부 투과유동을 무시**했다. 실제 응집체는 내부로 공기가 지나가 항력이 작아지므로,
  실제 오분류는 이 계산보다 크다.
- 부착 확률 `Bo/(1+Bo)` 는 Bo ≫ 1 에서 포화한다. 그래서 **점착성 가정에 둔감**하다 —
  EVA 배수를 1 → 8 로 8 배 키워도 품위는 97.6 → 97.3 % 로 거의 변하지 않는다.
  가장 불확실한 파라미터가 결과를 좌우하지 않는 점은 다행이지만, 뒤집어 말하면
  **이 모델은 "매우 끈적함"과 "끈적함"을 구별하지 못한다.** 실제 점착이 심하면 예측이 빗나간다.
- 체(SV-01) 단계의 응집은 별도 문제다. 응집체는 **포락 입경으로 체질**되므로 미분이
  굵은 분획에 실려 넘어간다. 여기서는 분급기 공급물만 다뤘다 — 그 결과가 공급물에
  어떻게 나타나는지는 **§6.3.1 의 이월(leak)** 로 다뤘다.

#### 운전점은 옮겨가지 않는다

응집(분산효율 80 %)을 반영해 v_r 을 다시 최적화해도 TC-01 은 2.00 m/s 로, 이상 모델의
2.07 m/s 와 사실상 같다. **§6.4 의 기하평균 선택은 응집이 있어도 유효하다.**

---

### 6.7 3제품 물질수지 — 회수율이 어디서 결정되는가

> **Rev.6 정정** — 검수가 이 절의 옛 수치(P1 122.7 kg/h, 은 99.0 %)와
> §6.8 의 계산(은 80.5 %)이 **한 문서 안에서 모순**임을 지적했다. 원인은
> 이 절이 Rev.5 가 폐기한 '데크 효율 90 %' 가정을 그대로 쓰고 있었던 것.
> 이제 정밀 계산은 `sieve_sim.circuit()`(형상 반영 체+분급 물리) 하나이고,
> `three_product_balance()` 는 그 결과를 재현하도록 맞춘 **간이 수지**다.

Rev.6 성적 (형상 반영 분급 · 전제 정합 분포 · v 0.82 m/s · 시드 3 평균, 350 kg/h):

| 스트림 | 유량 |
|---|---|
| 공급 | 350.0 kg/h |
| CC-01 공급 (75~280 µm + 체가 놓친 미분) | ~150 kg/h |
| CC-01 경량측 (= SS-01 공급) | 99 kg/h |
| **P1 실리콘 + 은** | **111.9 kg/h** |
| **P2 구리 조립** | **33.1 kg/h** |
| **P3 백시트 · EVA** | **205.0 kg/h** |

| 제품 | 회수율 |
|---|---|
| 구리 → P2 | **93.4 ~ 98.6 %** (지배 손실: 상단 데크 통과) |
| 백시트·EVA → P3 | **96.9 ~ 98.2 %** (1.8 % 는 P2 로 — 품위 88.8 %) |
| 실리콘+은 → P1 | **90.3 %** (스캘핑 없으면 88.1 %) |

#### 은 손실은 구리가 아니라 백시트로 나간다

75 µm 데크를 빠져나가지 못한 미분은 분급기로 넘어간다. 거기서 어디로 가는지가 관건인데,
**예외 없이 경량측**이다.

| 실리콘 입경 | 종말속도 (중력장) | 커트 0.82 m/s 대비 |
|---|---|---|
| 30 µm | 0.065 m/s | 1/12.6 |
| 60 µm | 0.237 m/s | 1/3.5 |
| 75 µm | 0.347 m/s | 1/2.4 |

가장 불리한 75 µm 에서도 마진이 2.4 배라 응집이나 난류로 뒤집히지 않는다. 즉 **오분류된 은은 구리 제품을
오염시키는 것이 아니라 백시트 제품에 섞여 버려진다.**

#### 그래서 SS-01 스캘핑 시브를 단다

버려지는 은이 경량측 한 곳에 모여 있고, 그 스트림은 폴리머 75~500 µm 와 실리콘 < 75 µm 로
**입도가 완전히 갈라져 있다.** 체 하나면 되찾을 수 있다.

| 항목 | 사양 |
|---|---|
| 형식 | 원형 시브 단단, 초음파 35~40 kHz |
| 직경 / 개구 | **Ø900 / 75 µm** |
| 공급 | 99 kg/h (CC-01 경량측 전량) |
| 소요면적 / 유효면적 | 0.48 m² / 0.57 m² (여유 +20 %) |
| U/S | P1 합류 |
| O/S | P3 백시트 |

**같은 컷을 두 번 치는 것이 한 번을 크게 치는 것보다 낫다 — 다만 곱셈은 성립하지
않는다.** Rev.4 는 `1 − (1−η)²` 로 90 % 두 번 = 99 % 를 기대했지만, §6.8 의 체
거동 해석이 보여주듯 **되찾을 대상이 근접입자 구간에 몰려 있어** 두 번째 체의
실효 효율은 ~20 % 다. 그래도 은 회수율 88.1 → 90.3 % (+2.2 포인트)는 Ø900 체
하나 값으로는 남는 장사고, SV-01 을 Ø1500 으로 키우는 것보다 싸다.

> **은 회수율 +2.2 포인트.** Rev.4 가 기대한 +9 포인트에서 거듭 정정한다.

---

### 6.8 체분리 거동 수치해석 — 불규칙 형상 입자

Rev.4 까지 체는 "효율 90 %" 라는 한 숫자였다. 이 절은 그 값을 **계산해서** 검증한다.
`tools/sieve_sim.py`(통계 모델)와 `tools/sieve_dem.py`(입자 하나하나의 운동방정식)
두 갈래로 풀었다.

#### 모델 — 체는 중간축을 재고, 편평한 입자는 모로 서야 통과한다

입자를 3축 `L ≥ I ≥ S` 로 둔다. 사각 개구를 통과하려면 **중간축 I** 가 개구보다 작아야
하고, 편평한 입자는 데크 위에 **누워서** 흐르므로 통과하려면 모로 서야 한다.

```
1 회 제시당 통과 확률
  p = 개방면적비 · (1 − 눈막힘) × ((a − I)/a)²  ×  (S/I)^n
        ↑ 개구에 닿을 확률        ↑ Gaudin        ↑ 배향
유효 제시 횟수 = 가진 16 Hz × 체류 45 s ÷ 베드 6 층 = 120 회
```

| 물질 | I/L | S/I | 형상 |
|---|---|---|---|
| 구리 | 0.60 | 0.50 | 리본 파편 |
| 실리콘(+Ag) | 0.75 | 0.60 | 취성 각형 파편 |
| 백시트+EVA | 0.70 | 0.35 | 라미네이트 박편 |
| EVA | 0.75 | 0.45 | 연성 덩어리 |

> **3축 비는 가정값이고 이 해석에서 가장 불확실한 입력이다.** 다만 배향 지수를
> 0.5~1.5 로 바꿔도 은 회수율이 77.9~80.7 % 로 움직이므로 **결론은 뒤집히지 않는다.**

#### 결과 ① — 실제 컷은 개구보다 작다

Gaudin 항 `((a−I)/a)²` 는 개구에 가까울수록 급격히 떨어진다. 개구의 80 % 크기인 입자는
통과 확률이 **개구의 40 % 크기인 입자의 1/9** 이다. 그래서 "200 µm 데크" 의 실제 컷은
160 µm 근처이고, **150~200 µm 구리가 상단 데크를 넘지 못한다.**

| 상단 데크 개구 | 구리 질량 통과율 |
|---|---|
| 200 µm | 85.0 % |
| **250 µm** | **97.2 %** |
| 300 µm | 99.5 % |
| 355 µm | 99.9 % |

Rev.4 는 "200 µm 오버에는 구리가 없다" 고 보고 P3 로 직행시켰는데, 그 가정이 이 지점에서
깨진다. **상단 데크를 250 µm 로 키운다**(§0).

#### 결과 ② — 초음파는 정량적으로도 결정적이다

근접입자(개구의 0.8~1.3 배)가 개구에 박혀 눈막힘을 만든다. 이 스트림은 근접입자 비율이
**24 %** 로 높다.

| | 눈막힘 (250/106/75) | 은 회수율 | 구리 회수율 |
|---|---|---|---|
| 초음파 ON | 0.8 / 0.8 / 0.8 % | **80.4 %** | **97.3 %** |
| 초음파 OFF (볼 데크만) | 79.1 / 78.6 / 68.4 % | 62.6 % | 68.2 % |

> 눈막힘 속도상수 두 개는 1 원리 유도가 아니라, 잘 확립된 산업 사실
> **"100 µm 이하 건식 체는 초음파 없이 개방면적을 대부분 잃는다"** 를 모델이 재현하도록
> 맞춘 보정값이다. 파일럿 실측 대상이다(§10).

#### 결과 ③ — SS-01 의 기여는 생각보다 작다

Rev.4 는 "90 % 두 번이면 99 %" 라고 보아 9 포인트를 기대했다. 실제로는 훨씬 작다
— 되찾을 대상이 **근접입자 구간에 몰려 있어** 두 번째 체도 잘 통과시키지 못하기
때문이다. 그래도 설비비 대비 기여는 유효하므로 유지한다.

> **Rev.6 갱신** — 이 절의 첫 계산(은 80.5 %, SS-01 기여 4.8 포인트)은 실리콘
> 분포가 §1 전제와 모순인 채(§0.2)의 값이었다. 정합 분포 + 형상 반영 분급 +
> 데크별 베드 부하 보정의 확정값: **은 90.3 %, SS-01 기여 2.2 포인트**
> (88.1 → 90.3). 상단 데크 비교는 §0.5 로 옮겨 갱신했다 — 보정 유무 두 모델의
> 최악값 기준으로 **250 이 아니라 280 µm** 를 채택한다.

#### DEM 교차검증

`tools/sieve_dem.py` 는 같은 현상을 입자 하나하나의 운동방정식으로 푼다.
입자를 원판 2~3 개를 붙인 **강체 clump** 로 두어 회전과 배향이 자연스럽게 나오고,
체는 판정식이 아니라 **고정 원판(체선) 사이의 기하**로 표현했다. 접촉은 선형
스프링-대시팟 + Coulomb 마찰, 가진은 데크 좌표계의 관성력 `a_z = −Γ g sin(ωt)` 다.

영상은 `sim_out/sieve.mp4` 로 렌더링된다(`tools/render_sieve_video.py`).

---

## 7. 집진 및 부대

| 항목 | 사양 |
|---|---|
| CC-01 상부(경량측) | **474 m³/h** → 사이클론 → 백필터 (계통 1벌) |
| **분급 계통 합계** | **474 m³/h** (컬럼 1대) |
| SS-01 커버 환기 | 별도 지관, 미압 부압 (미확정) |
| SV-01 커버 환기 | 별도 지관, 미압 부압 유지 (풍량은 커버 체적·개구부로 산정 — 미확정) |
| 백필터 | 여과속도 ≤ 1.0 m/min (미분 35 µm 대응), 펄스제트 |
| 팬 | 분급 계통과 커버 환기 계통을 **별도 팬으로 분리**, 각각 인버터 |

**계통 분리는 선택이 아니다.** CC-01 의 컷 정밀도를 지키려면 v 를 ±5 % 이내로
잡아야 하는데, 커버 환기와 한 팬을 쓰면 커버 쪽 개구 변동이 그대로 커트를 흔든다.

미분 35 µm 를 다루므로 여과속도를 1.0 m/min 이하로 낮춘다. 풍량이 Rev.2 대비 2.1 배로
늘었으므로 **백필터 여과면적도 그만큼 키워야 한다** — 같은 여과속도를 유지하려면
여과면적 ≈ 429 / (1.0 × 60) = **7.2 m² 이상**.

---

## 8. 은에 대한 사실 — 물리선별로는 분리되지 않는다

Rev.3 까지는 "결합되어 있으니 안 된다"고만 적었다. 근거가 부실했으므로 계산해서 확정한다.

은을 머금은 실리콘 입자는 밀도가 올라가므로 **원리적으로는** 밀도 판별의 대상이다.
문제는 대비가 너무 작다는 것이다.

| 입자의 Ag 함량 | 입자 밀도 | 순 실리콘과의 등침강 입경비 |
|---|---|---|
| 8 wt% | 2,493 | **1.04** |
| 19 wt% | 2,738 | **1.09** |
| 33 wt% | 3,146 | **1.17** |
| 53 wt% | 3,962 | **1.32** |
| *(비교)* 구리 ↔ 백시트 | 8,960 / 1,200 | *3.42* |

§4 의 논리 그대로다 — 분급으로 밀도를 보려면 **분획이 등침강 입경비보다 좁아야** 한다.
구리는 3.42 라서 1.4~2.7 : 1 분획으로 충분히 잡히지만, 은은 **질량의 절반이 은인 입자조차
1.32 를 요구한다.** 현실적인 입자(수 wt%)는 1.04~1.09 다.

여기에 결정적인 제약이 겹친다. 은은 전부 **75 µm 이하**에 있는데 이 영역은 건식 체질이
되지 않는다(실용 하한 38 µm, 그것도 초음파 필수). **1.1 : 1 분획을 만들어야 하는데 애초에
어떤 분획도 만들 수 없는 구간이다.**

> 은 분리는 기계 선정 문제가 아니라 **존재하지 않는 분리축**을 요구하는 문제다.
> 건식 어느 물리선별로도 나오지 않는다.

습식 원심 선별(Falcon/Knelson 계열)은 유효밀도 대비가 커져(1.35 → 1.61) 입경비가
1.16 → 1.27 로 개선되고 강화중력이 크기 효과를 누른다. 그래도 **분리가 아니라 예농축**이며,
건식 라인에 탈수·폐수처리 계통이 통째로 붙는다. **본 설계는 채택하지 않는다** —
발주 요구가 은을 실리콘과 결합된 상태 그대로 한 제품으로 받는 것이기 때문이다(§0).

건식 라인의 역할은 **< 75 µm 실리콘+은 농축물을 만드는 것까지**이며, 은 회수는 그 다음이다.

| 경로 | 비고 |
|---|---|
| 습식 침출 (질산계) | Ag 회수의 표준 경로 |
| 열처리 후 침출 | 잔류 폴리머 제거 선행 |

본 설계의 성능 지표는 **은 회수율이 아니라 < 75 µm 분획으로의 은 이송률**로 잡는다 — Rev.4 기준 **99.0 %**(§6.7).

---

## 9. 구리 순도 — 에어분급만으로는 부족하다

에어분급은 **조립(rougher)** 이다. 다음 두 오염 경로가 남는다.

1. **실리콘 혼입 — 해소되었다.** 커트를 실리콘 밴드 위에 두었으므로 (144 µm 이하가
   경량측) 실리콘은 120 µm 까지만 존재해 **전량 경량측으로 간다.**
   대신 그 실리콘은 폐기측으로 가므로 은 손실이며, **Rev.4 는 이것을 SS-01 로 되찾는다**(§6.7).
2. **비구형 구리** — 구리 리본 파편이 납작하면 종말속도가 구형 가정보다 낮아 경량측으로 샌다.

| 정선 후보 | 적용성 |
|---|---|
| 코로나 정전선별 | 75~200 µm 에 실적 있음. 단 **실리콘은 반도체라 도체처럼 거동할 수 있어** 구리/실리콘 분리는 불완전 |
| 습식 중력선별 (원심형·미세 요동테이블) | 밀도비 3.6 배로 유효. 습식 도입 여부가 관건 |

**§10 파일럿에서 두 방식을 병렬 비교한 뒤 결정한다.**

---

## 10. 검증 계획 — 본기 발주 전 필수

| # | 항목 | 확정하는 것 |
|---|---|---|
| 1 | **입도별 재질 분포** (체분석 + SEM/EDS 또는 산분해 정량) | §1 분획 중량비 — 현재 전부 가정값 |
| 2 | **입자 형상 계수 실측** | §3 종말속도. 비구형이면 전부 재산출 |
| 3 | **소형 향류 컬럼 시험** (풍속 1변수 + 정류격자 유무) | §6.4 — 컷 0.82 m/s 와 형상 반영 밴드가 실제로 유지되는지 |
| 3-2 | **컬럼 vs 진동 에어테이블 병렬 시험** | §6.4 형상 게이트가 닫히면(구형에 가깝게 나오면) 에어테이블로 전환 |
| 3-1 | **분산기 효율 실측** (레이저 회절 또는 현미경 계수로 응집체 비율) | §6.6 — 구리 품위를 직접 결정한다. **파일럿 1순위** |
| 4 | **75 µm 데크 시간당 효율 저하 곡선** | §6.3 은 회수율. 초음파 유무 비교 |
| 4-1 | **분획별 오버사이즈 혼입률 실측** (각 분획을 상한 체로 재체질) | §6.3.1 — 분급기 정밀도보다 우선한다. **수입 기준 2 % 이하** |
| 4-2 | **CC-01 경량측의 < 75 µm 함량 실측** | §6.7 — SS-01 의 회수 대상량. 은 회수율을 직접 좌우 |
| 4-3 | **입자 3축 비 실측** (현미경 이미지 해석, 재질별 100 개 이상) | §6.8 — 체 거동 모델의 가장 불확실한 입력 |
| 4-4 | **눈막힘 진행 곡선** (초음파 ON/OFF, 시간당 개방면적) | §6.8 의 보정 속도상수 2 개를 실측으로 대체 |
| 4-5 | **상단 데크 개구 250 / 280 / 300 µm 비교 체질** | §0.5 — 베드 부하 모델의 보정 여부와 최적 개구를 실측으로 확정 |
| 5 | **응집 거동 관찰** (습도 30/40/50/60 % RH) | §5 분산기 사양 |
| 6 | 분진 폭발지수 (Kst, Pmax) | 방폭 등급 |
| 7 | 정전선별 vs 습식 중력선별 병렬 시험 | §9 구리 정선 방식 |

### 성능 목표 (잠정)

**Rev.4 — 목적함수가 회수율이므로 지표를 제품별 회수율로 바꾼다.** 품위는 후단이 감당하므로
구속조건이 아니고, 참고값으로만 둔다.

| 지표 | 목표 | 계산값(§6.7) |
|---|---|---|
| **구리 회수율 → P2** | **90 % 이상** | 93.4 ~ 98.6 % (모델 범위) |
| **백시트·EVA 회수율 → P3** | **95 % 이상** | 96.9 ~ 98.2 % |
| **실리콘+은 회수율 → P1** | **85 % 이상** | 90.3 % |
| 상단 데크 개구 | 250 µm | (200 µm 이면 구리 15 % 유실) |
| 초음파 정상 동작 시 눈막힘 | 5 % 이하 | 0.8 % |
| 분획별 오버사이즈 혼입률 (SV-01) | 2 % 이하 (§6.3.1) | — |
| *(참고)* 구리 조립 품위 | 구속 아님 | 98.6 % |

> **은 목표 85 %** — Rev.5 는 분포 모순(§0.2) 탓에 78 % 까지 내렸으나, 정합
> 분포의 계산값 90.3 % 에 5 포인트 여유를 두고 85 % 로 되올린다. 계산이 95 %
> 에 못 미치는 원인은 체의 성능이 아니라 **체 거동 자체(Gaudin·배향)**다 —
> 실리콘 질량의 11 % 가 애초에 75 µm 위에 있고, 개구에 가까운 입자는 어떤 체로도 잘
> 통과하지 않는다. 달성 불가능한 사양을 적어 두면 수입검사에서 분쟁만 생긴다.
>
> **구리·백시트는 여전히 손댈 데가 거의 없다.** 다만 그것도 **상단 데크가 250 µm 일 때**의
> 이야기이며, 200 µm 이면 구리 회수율이 82.9 % 로 떨어진다.

---

## 11. 안전

미분 35 µm 를 다루므로 Rev.1 보다 분진폭발 위험이 높다. 실리콘 미분은 가연성 분진이다.

- 사이클론·백필터·분급기 케이싱에 **방폭 벤트**
- 전 계통 **본딩·접지 ≤ 10 Ω** — 밀폐 분급기는 정전 축적이 쉽고, 회전 휠은 대전원이라 특히 중요
- 분급기 내부는 **도전성 재질 또는 도전 코팅** (아크릴 케이싱 금지)
- 착공 전 **Kst / Pmax / MIE(최소착화에너지) 시험** — 미분일수록 MIE 가 낮아진다
- 백필터 차압 상시 감시

---

## 12. 미확정 항목

아래가 확정되면 `tools/screen_sizing.py` 의 `CONFIG` 를 갱신하고 재실행한다.

1. **입도별 재질 분포** — §1 의 분획 중량비는 전부 가정값. 이것 없이는 시브 직경도 잠정
2. **입자 형상 계수** — 구형 가정으로 계산했다. 편평하면 §3·§4 전부 바뀐다
3. **은 함량과 부존 형태** — 유리(遊離) 은이 상당량이면 §8 의 "분리 불가" 결론이 그 부분에는 적용되지 않는다
4. 백시트·EVA 실제 밀도 (복합 조성비)
5. 75 µm 데크 실효 처리능력 — §6.2 의 0.20 t/h/m² 는 가정
6. 습식 공정 도입 가능 여부 — §9 구리 정선, §8 은 회수 경로
7. SV-01 커버 환기 풍량 — 커버 형상 확정 후 산정
8. 공급의 연속성 (배치 여부)
9. 설치 공간 — CC-01 컬럼은 층고(체류 구간 ~2 m)를 요구한다. 집진 풍량 474 m³/h 의 덕트·백필터 공간 포함
10. **SS-01 스캘핑 시브의 실제 효율** — §6.7 은 90 % 가정. 은 회수율 99 % 가 여기에 달려 있다
11. **CC-01 경량측의 < 75 µm 실제 함량** — SS-01 사이징의 근거값
12. **입자 3축 비 실측이 기종을 결정한다** — 형상 게이트(§6.4)가 닫히면 CC-01 대신 진동 에어테이블
