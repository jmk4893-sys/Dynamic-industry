# CLAUDE.md

이 저장소에서 작업하는 AI 어시스턴트(Claude Code 및 유사 도구)를 위한 안내서입니다.

## 프로젝트 개요

**Dynamic-industry** — "Dynamic industry Development."

이 저장소는 **초기 / 뼈대(skeleton) 단계**입니다. 현재 시점에는 프로젝트 기본 골격만
존재하며, 애플리케이션 소스 코드·의존성 매니페스트·테스트·빌드 설정은 아직 없습니다.
도구 설정(`.gitignore` 참고)은 **Python** 프로젝트를 대상으로 합니다.

> **이 파일을 정직하게 유지하세요.** 실제 코드·의존성·워크플로가 추가되면 아래 관련
> 섹션을 갱신하고 "아직 없음" 표기를 제거하세요. 저장소에 실제로 존재하지 않는 구조·
> 명령어·규칙을 문서화하지 마세요. 상상한 것이 아니라 지금 여기 있는 것을 기술하세요.

## 현재 저장소 구성

```
.
├── README.md      # 한 줄짜리 프로젝트 설명
├── .gitignore     # 표준 Python .gitignore (GitHub Python 템플릿)
└── CLAUDE.md      # 이 파일
```

현재는 소스 디렉터리, 패키지 매니페스트(`pyproject.toml`, `requirements.txt`,
`setup.py`), CI 설정, 테스트 스위트가 존재하지 않습니다.

## 언어 & 도구

`.gitignore`는 표준 Python 템플릿으로, 광범위한 도구를 미리 고려하고 있습니다. 아래
항목은 아직 실제로 연결(설정)되어 있지 않으며, 프로젝트가 성장할 때 채택할 규칙입니다.
`.gitignore`가 이미 대비하고 있는 항목에 맞추어 선정했습니다.

- **언어:** Python 3.
- **패키징 / 의존성:** `pip`(`requirements.txt`), `uv`(`uv.lock`), `poetry`, `pdm`,
  `pipenv` 중 어느 것이든 `.gitignore`가 대비하고 있습니다. 의존성을 추가할 때 하나를
  선택하고, 그 선택을 여기에 기록하세요.
- **가상 환경:** `.venv/`, `venv/`, `env/`, `ENV/`가 모두 무시(ignore)됩니다. 저장소
  루트의 `.venv/`를 권장합니다.
- **테스트:** `pytest`를 상정합니다(`.pytest_cache/`, 커버리지 산출물이 무시됨).
- **타입 검사:** `mypy`, `pyre`, `pytype` 캐시가 무시됩니다.
- **린팅 / 포매팅:** `ruff` 캐시(`.ruff_cache/`)가 무시됩니다 — Ruff를 린터/포매터로
  권장합니다.

### 자주 쓰는 명령어 (프로젝트 설정 이후)

아래 명령은 **아직 실행할 수 없습니다** — 매니페스트나 코드가 없기 때문입니다. 예상되는
형태로만 사용하고, 도구가 추가되면 실제 실행 명령으로 갱신하세요.

```bash
# 환경 구성
python -m venv .venv && source .venv/bin/activate

# 설치 (선택한 패키징 도구에 따라 다름)
pip install -r requirements.txt      # 또는: uv sync / poetry install / pdm install

# 테스트
pytest

# 린트 & 포매팅
ruff check .
ruff format .

# 타입 검사
mypy .
```

실제 진입점(entry point)이 생기기 전까지 실행 명령을 임의로 만들지 마세요.

## 시크릿 & 환경

- `.env`, `.envrc`, `local_settings.py`는 git에서 무시됩니다 — **절대 시크릿을 커밋하지
  마세요.**
- `db.sqlite3`와 `*.log`는 무시됩니다. 로컬 데이터베이스와 로그는 버전 관리에서
  제외됩니다.

## Git 워크플로 & 규칙

- **기본 브랜치:** `main`.
- **AI 작업용 기능 브랜치:** `claude/claude-md-docs-f978f1` (이 문서 작업이 대상으로 하는
  브랜치). `main`이 아니라 지정된 기능 브랜치에서 개발하세요.
- **`main`에 직접 푸시하지 마세요.** 변경 사항은 기능 브랜치에서 진행하세요.
- **커밋:** 명확하고 서술적인 명령형 메시지를 사용하세요
  (예: "added stuff"가 아니라 "Add data ingestion module").
- **풀 리퀘스트:** 명시적으로 요청받았을 때만 PR을 여세요. `.github/`에 PR 템플릿이 있으면
  그 구조를 따르세요.
- **푸시:** `git push -u origin <branch-name>`.

## AI 어시스턴트를 위한 지침

1. **단정하기 전에 확인하세요.** 이 저장소는 뼈대 상태에서 빠르게 형태가 바뀝니다. 이
   문서의 내용에 의존하기 전에 실제 파일을 확인하세요(`ls`, `git status`, 매니페스트 읽기).
2. **요청된 범위로 작업을 한정하세요.** 과제가 요구하지 않는 한 프레임워크를 세우거나
   의존성을 추가하거나 디렉터리 구조를 만들지 마세요.
3. **이 파일을 최신으로 유지하세요.** 실제 구조(패키지·테스트·빌드·CI)를 추가하면 해당
   섹션을 갱신하고 "아직 없음" 단서를 제거하세요.
4. **기존 규칙을 따르세요.** 코드가 생기면 외부 관례를 들여오지 말고 그 스타일·배치·관용을
   그대로 반영하세요.
5. **시크릿을 절대 커밋하지 마세요** — `.gitignore` 대상 파일도 마찬가지입니다.
